<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talkforum.talkforumserver.user.UserMapper">

    <insert id="addUser" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
         INSERT INTO user(email, password, name, role, used_invite_code) VALUES (#{email}, #{password}, #{name}, #{role}, #{inviteCode})
    </insert>

    <select id="getUserVOById" resultType="com.talkforum.talkforumserver.common.vo.UserVO">
         SELECT * from user where id = #{userId}
    </select>


    <select id="getUserVOByEmail" resultType="com.talkforum.talkforumserver.common.vo.UserVO">
        SELECT * from user where email = #{email}
    </select>

    <select id="getUserLoginInfoByNameOrEmail" resultType="com.talkforum.talkforumserver.common.entity.User">
        SELECT * FROM user WHERE name = #{nameOrEmail} OR email = #{nameOrEmail}
    </select>

    <select id="getUserLoginInfoById" resultType="com.talkforum.talkforumserver.common.entity.User">
        SELECT * FROM user WHERE id = #{userId}
    </select>

    <update id="setUserProfile">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="intro != null">intro = #{intro},</if>
            <if test="avatarLink != null">avatar_link = #{avatarLink},</if>
            <if test="backgroundLink != null">background_link = #{backgroundLink},</if>
        </set>
        WHERE id = #{id}
    </update>


    <select id="countUserByNameOrEmail" resultType="int">
        SELECT COUNT(*) FROM user WHERE name = #{name} OR email = #{email}
    </select>

    <update id="updateUserStatus">
        UPDATE user SET status = #{status} WHERE id = #{userId}
    </update>

    <update id="resetUserPassword">
        UPDATE user SET password = #{hashPassword} WHERE id = #{userId}
    </update>

    <delete id="deleteUser">
        DELETE FROM user WHERE id = #{userId}
    </delete>

    <update id="setUserRole">
        UPDATE user SET role = #{role} WHERE id = #{userId}
    </update>


    <update id="updateLoginTime">
        UPDATE user SET last_login_at = NOW() WHERE id = #{userId}
    </update>
</mapper>